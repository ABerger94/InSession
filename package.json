{
  "name": "insession-marketplace",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack course marketplace for InSession.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.21.0"
  }
}
